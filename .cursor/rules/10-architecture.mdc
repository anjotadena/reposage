---
description: Architecture context for this repository (evidence-based; do not assume unstated structure).
globs: "*"
---

## Architecture (evidence-based)

### What we can say with confidence
- **Primary shape**: A **compiled Node.js CLI** (TypeScript-heavy codebase) with a **distribution entrypoint** at `dist/cli/index.js`.
- **Runtime entry points**:
  - **package `main`**: `dist/cli/index.js`
  - **CLI `bin`**: `dist/cli/index.js` (CLI name: `reposage`)
- **Ecosystem signals**:
  - **TypeScript is predominant** (by file count).
  - **ESLint** and **Prettier** are present.
  - **Strict TypeScript** is enabled (per analysis report).
- **CI/CD**: GitHub Actions workflow at `.github/workflows/ci.yml`.

### What is *not* supported by current evidence
- **Architecture style**: No framework/pattern was inferred; treat the architecture style as **unknown**.
- **Key modules / boundaries**: No module structure was inferred; do **not** invent layers (e.g., “core/services/adapters”) without inspecting the code.
- **Component connections / data flow**: No API routes, databases, or security architecture were detected; do **not** assume HTTP servers, persistence, or auth flows exist.

## Working assumptions for changes (keep these constraints)
- **Treat `dist/cli/index.js` as a build artifact** and the executable entrypoint; when debugging runtime behavior, start by tracing from this file.
- **When documenting architecture**, only promote claims that are directly verifiable from repository contents (e.g., imports, package metadata, directory layout). If you haven’t verified it, label it explicitly as “unknown”.
- **If you need architecture details**, first inspect:
  - `package.json` (scripts, bin/main, build tooling)
  - the source tree that produces `dist/cli/index.js` (compiler config and corresponding source entry)
  - how CLI commands are registered and dispatched (command router / argument parsing), once located