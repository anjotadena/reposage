---
description: Repository baseline for reposage (stack, structure, conventions).
globs: "*"
---

## Detected stack
- **Languages**: TypeScript (`.ts`, `.tsx`), JavaScript (`.js`, `.jsx`, `.mjs`, `.cjs`), JSON (`.json`), Markdown (`.md`)
- **Frameworks**: none detected (low confidence)
- **Testing**: no test framework detected (low confidence)
- **CI/CD**: GitHub Actions (`.github/workflows/ci.yml`)
- **Entry points**: `dist/cli/index.js` (package.json `main` and `bin`)

## Architecture
- **Style**: Unknown (no architecture pattern inferred from detected frameworks)

## Conventions to follow
- **Formatting**: Prettier (`.prettierrc`)
- **Linting**: ESLint (`eslint.config.js`)
- **TypeScript**: strict TypeScript enabled
- **CI**: keep changes compatible with GitHub Actions workflow(s)

## Follow existing patterns
- Prefer the existing CLI structure and build output conventions (notably `dist/cli/index.js`).
- Match the repository’s existing lint/format/type-check expectations; don’t introduce new tools/config styles unless already present.