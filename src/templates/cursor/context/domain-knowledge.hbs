<!-- Generated: {{generatedAt}} | RepoSage {{version}} -->

# Domain Knowledge

Key terms, concepts, and patterns for agents and automations.

## Project Domain

{{#if report.metadata.targetRepository}}
Repository: `{{report.metadata.targetRepository}}`
{{/if}}

## Key Terms

{{#if report.modules.data}}
### Module Names
{{#each report.modules.data}}
- **{{name}}**: Located at `{{path}}`
{{/each}}
{{/if}}

## Naming Patterns

{{#if report.languages.data}}
Based on detected languages ({{#each report.languages.data}}{{name}}{{#unless @last}}, {{/unless}}{{/each}}):
- Follow language-specific naming conventions
- Match existing patterns in the codebase
{{/if}}

## Domain Concepts

Infer domain concepts from:
- Module and directory names
- API route patterns
- Test file organization

{{#if report.apiRoutes.data}}
### API Concepts
{{#each report.apiRoutes.data}}
- Route: `{{path}}` suggests domain entity or action
{{/each}}
{{/if}}

## Acronyms & Abbreviations

Document project-specific acronyms here:
- Add terms as they're discovered during development

## Patterns to Follow

1. **Naming**: Match existing file and function naming
2. **Structure**: Follow established directory organization
3. **API**: Maintain consistent route patterns
4. **Testing**: Mirror existing test organization

## For Automations

When automations encounter unfamiliar terms:
1. Check this file first
2. Search related modules for context
3. Flag for manual clarification if still unclear
